import StrategyBuilder from "@/components/StrategyBuilder";

export const metadata = {
  title: "Add Strategy | AI Crypto Backtest",
};

export default function NewStrategyPage() {
  return <StrategyBuilder />;
}
